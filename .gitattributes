# 统一换行：文本一律 LF（跨平台协作；Windows 上也不会再把 diff 弄成整文件重写）
* text=auto eol=lf

# 二进制（GitHub 上按图预览，不做换行转换）
*.png binary
*.jpg binary
*.ico binary
*.tgz binary

# 生成物/锁文件不参与语言统计
*.md linguist-documentation
docs/assets/* linguist-vendored
